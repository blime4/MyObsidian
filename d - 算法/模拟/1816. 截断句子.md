```c++

class Solution {
public:
    string truncateSentence(string s, int k) {
        int cnt = 0;
        for(char val : s){
            if(val==' ') k--;
            if(k==0) break;
            cnt++;
        }
        return s.substr(0,cnt);
    }
};

```